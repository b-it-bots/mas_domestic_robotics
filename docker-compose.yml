version: '2'
services:
  travis:
    image: bitbots/bitbots-domestic:${TRAVIS_COMMIT}
    build:
      dockerfile: .travis/Dockerfile
      context: .
  domestic:
    build: .
    image: bitbots/bitbots-domestic:melodic
