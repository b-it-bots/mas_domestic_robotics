<launch>

  <!-- Set the file describing which sensor(s) to use to build the octomap  -->
  <rosparam command="load" file="$(find mdr_moveit_cob)/config/sensors_rgbd.yaml" />
  <rosparam command="load" file="$(find mdr_moveit_cob)/config/octomap.yaml" />

</launch>
