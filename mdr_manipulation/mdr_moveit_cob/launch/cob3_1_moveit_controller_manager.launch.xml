<launch>

  <!-- Set the param that trajectory_execution_manager needs to find the controller plugin -->
  <param name="moveit_controller_manager" value="simple_moveit_controller_manager/SimpleMoveItControllerManager"/>

  <!-- The rest of the params are specific to this plugin -->
  <rosparam file="$(find mdr_moveit_cob)/config/controllers.yaml"/>

</launch>
